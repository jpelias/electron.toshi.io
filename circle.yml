machine:
  python:
    version: 2.6
dependencies:
  pre:
    
    - pip install ecdsa
  #  - pip install numpy
    - pip install gmpy
    
test:
  override:
      
    - python electron.py 
    - python electron.py 
    
